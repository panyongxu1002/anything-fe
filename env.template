# X402 Payment Gateway URL (public - used by both client and server)
NEXT_PUBLIC_X402_GATEWAY_URL=https://x402.bedev.hubble-rpc.xyz/lego/api/v1/query

# Client Private Key (SERVER-SIDE ONLY - for x402 payments)
# ⚠️ CRITICAL SECURITY: 
# - This is a SERVER-SIDE environment variable (no NEXT_PUBLIC_ prefix)
# - Never expose this to the browser
# - Only accessible in API routes and server components
# - Must be a valid Ethereum private key with USDC balance on Base Sepolia
# Format: 0x followed by 64 hexadecimal characters
CLIENT_PRIVATE_KEY=0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef
